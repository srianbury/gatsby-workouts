import React from "react";
import Layout from "../components/layout";

const About = ({ location }) => {
  return (
    <Layout title="About" url={location.href}>
      I'll get to this later... maybe...
    </Layout>
  );
};

export default About;
